replicas: 2

image:
  repository: "opensearchproject/opensearch"
  tag: "2.18.0"

config:
  opensearch.yml:
    plugins:
      security:
        system_indices.enabled: true
      ml_commons:
        agent_framework_enabled: true
